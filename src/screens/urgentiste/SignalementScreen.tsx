import SignalementScreenModular from './signalement/index';

/**
 * Proxy component to maintain backward compatibility with named imports
 * while using the new modular structure.
 */
export const SignalementScreen = SignalementScreenModular;
export default SignalementScreenModular;
